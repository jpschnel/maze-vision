#============================================
.SUFFIXES:
#============================================
CC=
TOPDIR=
FLAGS=
SOURCES=
OBJECTS=
OTHERSTUFF=
EXECUTABLE=
#============================================
#RESERVED FOR VARIABLE DETAILS
#Might wanna do an include on the dslr libs
#============================================

all: $(SOURCES) $(EXECUTABLE)

$(EXECUTABLE): $(OBJECTS)
#	$(CC) $(FLAGS) $()
#   Suppose we should figure what we want with this later AKA what Lang or whever

clean:
	rm $(EXECUTABLE)